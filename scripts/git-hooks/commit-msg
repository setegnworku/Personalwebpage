#!/bin/sh
# Remove Cursor co-author trailer after git commit --trailer injection.
MSG_FILE="$1"
if [ -f "$MSG_FILE" ]; then
  sed -i.bak '/cursoragent@cursor\.com/d' "$MSG_FILE" 2>/dev/null || \
  sed -i '' '/cursoragent@cursor\.com/d' "$MSG_FILE" 2>/dev/null || true
  rm -f "${MSG_FILE}.bak"
fi
