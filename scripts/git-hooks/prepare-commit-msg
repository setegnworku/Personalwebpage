#!/bin/sh
# Strip Cursor co-author lines from commit messages (local hook).
MSG_FILE="$1"
if [ -f "$MSG_FILE" ]; then
  sed -i.bak '/cursoragent@cursor\.com/d' "$MSG_FILE" 2>/dev/null || \
  sed -i '' '/cursoragent@cursor\.com/d' "$MSG_FILE" 2>/dev/null || true
  rm -f "${MSG_FILE}.bak"
fi
